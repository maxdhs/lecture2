// write an arrow function for add
export const add = (a, b) => {
  return a + b;
};
export const subtract = (num1, num2) => {
  return num1 - num2;
};

const divide = (num1, num2) => {
  return num1 / num2;
};

export default divide;
